# Custom Rules

- All the mentioned .csv is not on the local file system, do not search for it
- Never test the website unless explicitly required to do so
- The current repo the AI is editing is the code machine, there is no transformer lib or venv in it, do not look for it. It's all there in the compute node.
- Never read the .env file
